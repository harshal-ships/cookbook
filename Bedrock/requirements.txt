# Amazon Nova Sonic 2 (bidirectional streaming SDK)
# Install from GitHub — this is an experimental AWS SDK:
#   pip install aws-sdk-python-bedrock-runtime
# or:
#   pip install git+https://github.com/awslabs/aws-sdk-python.git#subdirectory=codegen/aws-sdk-bedrock-runtime
aws-sdk-python-bedrock-runtime

# Telcoflow — phone call audio I/O
telcoflow-sdk>=0.24.0

# Configuration
python-dotenv
