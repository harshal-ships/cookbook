# Telcoflow SDK is on TestPyPI only; everything else resolves from PyPI.
# https://docs.telcoflow.com/introduction/overview
--extra-index-url https://test.pypi.org/simple/

# Telcoflow — phone audio (aria_bedrock_agent.py)
telcoflow-sdk>=0.24.0
python-dotenv>=1.0.0

# Amazon Nova Sonic 2 (bidirectional stream) — not on PyPI; needs Python >=3.12
git+https://github.com/awslabs/aws-sdk-python.git#subdirectory=clients/aws-sdk-bedrock-runtime
