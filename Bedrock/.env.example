# Telcoflow — phone call connectivity
WSS_API_KEY=your_telcoflow_api_key
WSS_CONNECTOR_UUID=your_telcoflow_connector_uuid

# AWS credentials for Nova Sonic 2 on Bedrock
# IAM credentials are REQUIRED — Bedrock API Keys do not support
# bidirectional streaming (InvokeModelWithBidirectionalStream).
AWS_ACCESS_KEY_ID=your_aws_access_key
AWS_SECRET_ACCESS_KEY=your_aws_secret_key
AWS_REGION=us-east-1
