# Telcoflow SDK is on TestPyPI only; everything else resolves from PyPI.
# https://docs.telcoflow.com/introduction/overview
--extra-index-url https://test.pypi.org/simple/

# Telcoflow — phone audio (crew_agent.py)
telcoflow-sdk>=0.24.0
python-dotenv>=1.0.0

# Gemini Live:stay on 1.x (langchain-google-genai in the stack requires google-genai<2)
google-genai>=1.75.0,<2

# Triage — CrewAI `LLM(model="gemini/...")` (no [google-genai] extra → fewer resolver conflicts)
crewai>=1.0.0
